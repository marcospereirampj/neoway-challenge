Return all data from databese
---
tags:
  - data-integration
definitions:
  Data:
    type: object
    properties:
      name:
        type: string
      addresszip:
        type: string
      website:
        type: string
parameters:
- in: query
  name: search
  schema:
    type: string
  required: false
  description: Key for search in 'name' and 'addresszip'
- in: query
  name: scroll_id
  schema:
    type: string
  required: false
  description: The next elements from result
produces:
  - application/json
responses:
  200:
    description: List all objects from database
    schema:
      $ref: '#/definitions/Data'